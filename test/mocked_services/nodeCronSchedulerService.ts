import { IScheduler } from "../../src/application/interfaces/IScheduler";
const nodeCronSchedulerService: IScheduler = {
  async schedule(expression, task) {},
};
export { nodeCronSchedulerService };
